apiVersion: v1
kind: Service
metadata:
  name: earningsiq-frontend
  namespace: earningsiq
spec:
  type: NodePort
  selector:
    app: earningsiq-frontend
  ports:
    - port: 5173
      targetPort: 5173
      nodePort: 30073
